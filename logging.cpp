#include "logging.hpp"

/*void log_text(const std::string text){
	log_line("%s", text.c_str());
}

void log_line(const std::string format_string, const std::string text){
	fprintf(stderr, "\n");
	fprintf(stderr, format_string.c_str(), text.c_str());
	fflush(stderr);
}

void print_ts()
{
    time_t ltime; // calendar time
    ltime=time(NULL); // get current cal time
    fprintf(stderr,"%s",asctime( localtime(&ltime) ) );
}*/